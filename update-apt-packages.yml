- hosts: all
  become: true
  tasks:
    - name: Update apt packages
      apt:
        upgrade: yes
    
    - name: Install basic packages
      package:
        name: "{{ item }}"
        state: present
      loop:
        - neofetch